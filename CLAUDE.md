# CLAUDE.md — garden-agent

# Git rules — CRITICAL
- NEVER run `git commit` or `git push` (or any destructive git command) without explicit user permission in that message.
- Always show what would be committed and ask the user before running `git commit`.
- Always ask the user before running `git push`.
